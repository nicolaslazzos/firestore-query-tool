import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

//con ese metodo render, se agregar al ReactDOM, el componente App, dentro del elemento con id = "root"
ReactDOM.render(<App />, document.getElementById('root'));
